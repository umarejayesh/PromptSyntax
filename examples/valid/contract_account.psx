target Java17;

entity Account {
    balance : int;

    method withdraw(amount : int) : boolean {
        requires "amount > 0";
        ensures "balance >= 0";
    };
}

generate {
    class;
    constructor;
    getters;
    tests;
}

verify {
    compile;
    run_tests;
}